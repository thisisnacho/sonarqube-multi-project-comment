sonar.projectKey=multi-comment-demo-beta
sonar.projectName=multi-comment demo / beta
sonar.sources=src
sonar.sourceEncoding=UTF-8

# Suppress SonarCloud's native per-project PR comment; this action posts
# the aggregated one instead.
sonar.pullrequest.github.summary_comment=false
